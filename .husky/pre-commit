#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# If this fails, see the "Troubleshooting" section in the README.

make check-node-version

# keep docs up to date
node tools/scripts/maintain-readme/index.mjs
git add README.md

corepack pnpm lint-staged
