import { Meta, Canvas, Controls } from '@storybook/blocks';
import IconStories, {
	XsmallIconsDefaultTheme,
	PaymentIconsDefaultTheme,
	WidePaymentIconsDefaultTheme,
} from './Icons.stories';
import { IconGallery, IconItem } from './IconGallery';

<Meta of={IconStories} />

# Icons

## Core

<IconGallery>
	{XsmallIconsDefaultTheme.args.icons.map((Icon) => (
		<IconItem name={Icon.name} key={Icon.name}>
			<Icon size="medium" />
		</IconItem>
	))}
</IconGallery>

## Payment

<IconGallery>
	{PaymentIconsDefaultTheme.args.icons.map((Icon) => (
		<IconItem name={Icon.name} key={Icon.name}>
			<Icon size="medium" />
		</IconItem>
	))}
</IconGallery>

## Wide payment

<IconGallery>
	{WidePaymentIconsDefaultTheme.args.icons.map((Icon) => (
		<IconItem name={Icon.name} key={Icon.name}>
			<Icon size="medium" />
		</IconItem>
	))}
</IconGallery>
