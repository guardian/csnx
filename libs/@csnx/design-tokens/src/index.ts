import { tokens } from './build';

const palette = tokens.palette;
const breakpoints = tokens.breakpoint;
const space = tokens.space;
const remSpace = tokens.remSpace;

export { palette, space, remSpace, breakpoints };
