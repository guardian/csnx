---
'@guardian/source-react-components': minor
---

Adds optional `size` prop to `TextInput`, `TextArea`, `Label`, `InlineError` and `InlineSuccess` components. `medium` is the default value if `size` is not set, matching current behaviour.
