name: Deploy Storybooks
on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest

    # For RR upload
    permissions:
      id-token: write
      contents: write

    steps:
      - uses: actions/checkout@v3
      # enables use to use the cache in actions/setup-node
      - uses: pnpm/action-setup@v2.2.4
      - uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - run: make build-storybooks

      # bundle RiffRaff + deploy
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: eu-west-1
          role-to-assume: ${{ secrets.GU_RIFF_RAFF_ROLE_ARN }}

      - name: Deploy project storybooks AWS
        uses: guardian/actions-riff-raff@v2
        with:
          app: csnx:storybooks
          config: |
            stacks:
              - deploy
            regions:
              - eu-west-1
            allowedStages:
              - PROD
            deployments:
              storybook-source-foundations:
                type: aws-s3
                parameters:
                  bucket: com-gu-storybook-source-foundations
                  cacheControl: private
                  publicReadAcl: true
                  prefixPackage: false
                  prefixStack: false
                  prefixStage: false
              storybook-source-react-components-development-kitchen:
                type: aws-s3
                parameters:
                  bucket: com-gu-storybook-source-react-components-development-kitchen
                  cacheControl: private
                  publicReadAcl: true
                  prefixPackage: false
                  prefixStack: false
                  prefixStage: false
          contentDirectories: |
            storybook-source-foundations:
              - dist/storybook/libs/@guardian/source-foundations
            storybook-source-react-components-development-kitchen:
              - dist/storybook/libs/@guardian/source-react-components-development-kitchen
      - name: deploy react components to s3
        run: pnpm storybook-to-aws-s3 --bucket-path=com-gu-storybook-source-react-components --aws-profile=${{ secrets.GU_RIFF_RAFF_ROLE_ARN }}
      - name: deploy root storybook to github pages
        run: pnpm storybook-to-ghpages --ci --existing-output-dir dist/storybook/apps/csnx
        env:
          GH_TOKEN: ${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}
